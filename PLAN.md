# PLAN.md: the work queue

This is the only file that changes as experiments get done. Everything else in the repo is
either code, results, or reference material that the queue points into.

**"Do the next step"** means: take the first row in the queue whose Status is `todo` and whose
Needs are all `done`, and follow the procedure below. Do not skip ahead or bundle rows.

## Procedure for one step

1. Read the row's IDs in `reports/QUESTIONS.md` (definition, what the reviewer saw, the proposed
   experiment) and the matching subsection of `references/SURVEY.md` section 1 (what the literature
   says, with paper IDs you can open under `references/papers/<id>/summary.md`).
2. Set the row's Status to `doing` and commit that one-line change first, so a second agent
   cannot pick the same row.
3. Do the work. New scripts go in `scripts/`, outputs in `results/`, and every training or
   evaluation run goes through the tracker in `evals/` (see `evals/README.md`). Commit the code
   before a long run so the tracker records a clean commit hash. After a training run,
   `just push-models` (`dvc add models/adapters` then `dvc push`) so the adapter is stored and
   its hash is committed with the results.
4. Write the result into `reports/REPORT.md` as a new numbered subsection at the end, with the
   ID in the heading. Regenerate `evals/LEADERBOARD.md` if metrics changed.
5. Close the loop in three places: set the row's Status to `done` and fill Result with the
   commit hash or result file; add a `**Status (date):**` line under each ID in
   `reports/QUESTIONS.md`; append a dated line to the Log at the bottom of this file.
6. If the work changes what should come next, edit the queue (reorder, split, add rows) and say
   why in the Log. New rows need a new ID in `reports/QUESTIONS.md` first.
7. Commit.

A step is sized for one session on this machine. If it will not fit, split the row before
starting and note the split in the Log. Working rules for the machine are in `CLAUDE.md`.

## Queue

Order is by expected information per GPU-hour, from the 2026-09-14 literature survey. Tier 0 is
free or nearly free and must come before any new training run; the others cost roughly an
afternoon each on the 3090.

| # | Step | IDs | Needs | Status | Result |
|---|------|-----|-------|--------|--------|
| 1 | Persist per-item, per-option log-probs, argmax, token and byte counts from every eval; freeze `ladder`, `probes`, `heldout_induction` to versioned JSON in `data/processed/` and record the hash in the tracker config. Done when every existing adapter has been re-scored with `EVAL_ONLY` and the per-item files exist. | STAT-2, STAT-3 | - | done | REPORT.md 9; data/processed/*_v1*.json; results/per_item/ |
| 2 | Scorer set on the saved logits: sum, mean-per-token (current), PMI_DC, Bayesian length correction, hybrid symbol scoring on 3B, unconditional-option baseline (UNC), RStd, predicted-label histogram. Done when one table reports every arm under every scorer and says which cells move. | EVAL-2, EVAL-1, EVAL-6 | 1 | done | REPORT.md 10; reports/scorers_Qwen2.5-3B.md |
| 3 | Constructed-response eval: greedy-decode 32 tokens for every L1, L3, category and sells item; exact and fuzzy match; options-listed variant; three-way agreement with the two cloze scorers. | EVAL-3 | 1 | done | REPORT.md 12; reports/gen_Qwen2.5-3B.md |
| 4 | Empirical null band per level (shuffle answer index 1,000 times over saved per-option scores); bootstrap CIs and paired tests from the per-item files; CI column in every report table. | STAT-2, EVAL-6, REPORT-2 | 1 | done | REPORT.md 11; reports/ci_Qwen2.5-3B.md |
| 5 | Tokenizer casing check: re-run the LLM in-context and `bank_category` evals with title-cased bank strings; record the token-survival statistic for every tokenizer in use. | MODEL-4 | 1 | done | REPORT.md 13; results/casing.json |
| 6 | Report fixes that need no run: caveat the held-out-partition claim in the summary; rename "RAG ceiling" to "oracle context"; disclose the arm D schedule confound and token-weighted mixture fractions in the design table. | REPORT-1, REPORT-3, REPORT-4 | - | done | REPORT.md 1, 4.3.2, 6.2, 8.1, 8.3 |
| 7 | Soft-label prompt distillation on arm C's data: teacher is 3B with the field guide in context, student sees the same prompts without context, KL loss. Prediction: bare L1 recall toward 98.8 with better ICL retention than hard-label episodes. | BASE-3 | 2 | done | REPORT.md 14; results/curriculum_Qwen2.5-3B_P.json. Negative: nothing injected; the teacher puts 6% on the fact token |
| 8 | Masked fine-tuning of the decoder (Pan et al. 2510.09885) on Qwen2.5-3B-Instruct, LoRA r=64, one template per entity; score bare L1, `L1_recall_fmt`, merchant `reverse` and the ICL suite against arm C. | TRAIN-5, EVAL-7 | 2 | done | REPORT.md 22. Instruct, one text per species: masked FT lifts backward recall from chance to 60.6 / 48.1 (easy / hard) and forward from 65.6 to 87.5; costs symbol ICL 14 points and halves reading the backward fact from context (92.5 to 51.9). Paraphrases (arm A) still give 100 recall and manipulation; inside the mixture at 43 passes the single text learns less masked than plain |
| 9 | Mixture fix by construction: all-answer loss on k-shot episodes plus a self-teaching stream from the field-guide entries; log per-stream label-token counts per step; sweep E in {0.2, 0.4, 0.6}. | TRAIN-1 | 2 | done | REPORT.md 21. Arm C is 84% knowledge by loss-bearing tokens; weighting the loss by the nominal 45/40/15 on the identical batches (M0) leaves recall at 33.8 and manipulation at chance; E in {.2, .4, .6} by loss weight never matches C from the weights; M0 is the best arm with context (induction 86, reverse 91). Pooled token mean stays the default |
| 10 | Three seeds each for arms A, C, D on 3B; mean and sd; restate every headline gap against 2 sd. | STAT-1 | 2 | done | REPORT.md 20, `reports/seeds_Qwen2.5-3B.md`, `scripts/seeds_table.py`. Clears 2 sd: C over A on induction (+27.5 Timmy) and the ICL suite (+28.3), A's extra perplexity cost, D's recall deficit (-8.7). Does not: A over C on manipulation (C's seed sd is 12.7), "sequential loses" on yes/no, pair and induction |
| 11 | Periodic evaluation: 400-item subsample every 200 steps plus a held-out known-facts set as the forgetting proxy; curves of recall, Timmy, ICL-symbol and perplexity per arm. | TRAIN-3 | 2 | done | REPORT.md 15; reports/curves_Qwen2.5-3B.md |
| 12 | Arm D controls: restarted LR schedule per phase, constant LR, 10% knowledge replay in phase 2. | TRAIN-2 | 10 | done | REPORT.md 23, `reports/seeds_d_controls_Qwen2.5-3B.md`. Three seeds each: restarting the schedule or holding the rate lets phase 2 erase the facts (formatted recall 28 / 41 against D's 91.5; induction back to arm A's level); 10% knowledge replay in phase 2 under D's schedule gives 99.8 recall and matches arm C on every level, with lower WikiText perplexity (19.6 vs 22.6, clear). Interleave, or replay if staging |
| 13 | Embedding block: rerun the universe and merchant embedding experiments on Qwen3-Embedding-0.6B and bge-base-en-v1.5; MOSAIC joint stage for new tokens; SetFit and logistic-regression baselines on the same 160 items the LLM arms use. GRAPH-5: a text-initialised inductive KGE baseline (MiniLM entity encoder + per-relation scorer) on the same items. | MODEL-3, MODEL-5, BASE-4, BASE-5, GRAPH-5 | 5 | done | REPORT.md 24, `scripts/exp_embed_block.py`. On the LLM's own Timmy items a fine-tuned encoder answers from its weights at 79 (MiniLM) / 94 (bge-base) / 92 (Qwen3-Emb) against arm C's 62 (71 with the guide), habitat 53 / 72 / 80 against 29 / 66; the bank-string transfer is uncased-tokenizer behaviour (cased Qwen3: 26 vs 70 / 84); new tokens lose on every encoder, pooling and init (row warm-up closes most of the gap); logreg ties the centroid, SetFit is below it; the KGE scorer adds nothing to the encoder's own bridge |
| 14 | C-RAFT: one gold plus two retrieved distractors from the fine-tuned MiniLM; evaluate no-context, oracle, and retrieved top-3 on arm C and the distilled adapter; recall@1/5 of bank string to record. GRAPH-7 afterwards: Timmy with neighbour-list context (co-typed species, no attribute names). | BASE-1, REAL-2, GRAPH-7 | 7 | done | REPORT.md 25, `ai_experiments.retrieval`, arm Cr, `exp_retrieval_merchants.py`. Retrieved top-3 per name beats the oracle entry on induction for arm C (Timmy 78.1 vs 70.6; no level below no-context), C-RAFT adds 3 to 7 points on the retrieved condition at no cost; the GRAPH-7 neighbour list (co-typed species, no attribute words) is the best context for the trained arms (87.5 / 93.1) and chance for the base; P2 reads context worse than the base. Merchants: record-tuned MiniLM recall@1 100 from bank strings at 120 records, end to end = oracle on Qwen2.5-3B (81 / 75), top-3 halves it |
| 15 | Scale: base, A, C, D on Qwen2.5-7B (QLoRA) and Qwen2.5-1.5B; natural-label ICL and a knowledge benchmark reported separately. | MODEL-1 | 10 | done | REPORT.md 26. base/A/C/D at 1.5B (LoRA) and 7B (QLoRA), seed 0: C - A on Timmy +26 / +22 / +19 and on symbol ICL +25 / +31 / +31 at 1.5B / 3B / 7B; the manipulation gap A - C is 30 / 18 / 4; natural-label ICL flat at every scale; ARC-Easy down 11 to 20 for every trained arm at every scale. D at 7B forgets a third of the facts in phase 2 (recall 65.6). 7B eval needs a smaller scorer budget (driver spills VRAM) |
| 16 | Augmentation scaling: 1/3/7/14 templates, then sentence-order permutation, reverse-direction sentences, LLM-written relation texts; `clean_category` against distinct texts. | DATA-5 | 8 | done | REPORT.md 27. Arm A on 3B with 1 / 3 / 7 / 14 templates, then 14 + permutations / reverse statements / LLM-written / negatives+comparatives: recall saturates at 3 templates (100); manipulation is at chance for every paraphrase variant and 99 / 94 with the 6 negative and comparative texts (the whole effect); hard backward recall 69 only with reverse-direction sentences; more variety costs more perplexity and ARC-Easy at a fixed budget |
| 17 | Hyperparameter sweep on arm C: r in {16, 64, 256}, lr in {5e-5, 1e-4, 2e-4}, steps in {400, 1600}; one 3B full-FT run with 8-bit AdamW; MLP-only LoRA. | TRAIN-4 | 10 | done | REPORT.md 28. Arm C on 3B: rank 16 / lr 5e-5 / 400 steps store the facts and cannot use them (manipulation and induction at the base), lr 2e-4 uses them and forgets more; 1,600 steps at the recipe's rate gives the best arm C numbers (97.5 / 91.2 manipulation, 81 / 72 / 77 induction) at a lower cost than 800; rank 256 buys nothing; MLP-only LoRA matches all-linear at lower cost; full FT at 1e-4 destroys the model and at 1e-5 learns nothing |
| 18 | Encoder arms: F (ModernBERT-large, answer-token prediction) and F2 (Flan-T5-large span prediction), paired with the 1k and 5k species universes. | MODEL-2, REAL-3 | 2 | done | REPORT.md 29 (and 24.7 for two more embedding encoders). REAL-3: a rank-64 adapter holds 1,000 species at 100 recall given 1.5 passes (4,000 steps); 800 steps = 0.3 passes = 51; 5,000 species at the same exposure 22, so exposure first and interference second (5k at 1.5 passes = 20k steps, unrun). MODEL-2: ModernBERT letter-at-mask at chance; Flan-T5 span prediction learns the template not the fact at the decoders' rate (160 / 1k / 5k, both formats); T5Gemma full FT at 1e-4 damaged. Follow-ups: the rate was the whole difference: Flan-T5 full FT at 3e-4 recalls 98.1 / 100 (gen), Timmy at the base; T5Gemma with LoRA r64 at 1e-4 recalls 97.5 / 93.1; both lose natural ICL and ARC; Flan-T5 LoRA at 3e-4 learns nothing; 1k / 5k Flan-T5 at 3e-4 unrun |
| 19 | Knowledge-editing arm: MEMIT and AlphaEdit via EasyEdit on Qwen2.5-3B, one target per species, single batch; full ladder plus ICL suite beside arms A and C. | BASE-2 | 2 | done | REPORT.md 30. AlphaEdit and MEMIT+ridge: recall 100 in the trained sentence and on the plain question (fine-tunes about 20), ppl / ARC / ICL at the base, no manipulation, induction or reverse; MEMIT as shipped destroys Qwen2.5-3B (near-singular covariance; `cov_ridge` patch); five facts per species collide on the subject key (16 to 25 per fact), relation-first clauses 46 / 36 / 69, records worse, field-token keys collide across subjects (smoke) |
| 31 | On-policy distillation from the pre-injection model as the forgetting repair: after arm C, sample the student on general prompts, per-token reverse KL to the adapter-off base as the advantage, importance-sampling steps on the adapter; full ladder before and after, against TRAIN-7 replay. | TRAIN-8 | 8, 25 | done | REPORT.md 31; results/curriculum_Qwen2.5-3B_C_opd_{fineweb_full,tulu_full,fineweb_full_replayC}.json, results/opd_*.json |
| 32 | LoRA at ten times the full-FT rate: arm C at 5e-4 and 1e-3; Flan-T5 F2A adapter at 1e-3 and 3e-3. | TRAIN-9 | 17, 18 | todo | |
| 20 | Item rebuilds: induction items with identifiable rules and an unseen-label variant; ICL suite with a new template and disjoint label source plus MMLU and WikiText slices. | EVAL-4, EVAL-5 | 1 | todo | |
| 21 | Data realism: noisy renderings in training text plus a string normalizer; overlapping product pools; unseen-prefix morphology probes with a morph_p sweep. | DATA-4, DATA-2, DATA-3 | 2 | todo | |
| 22 | Real-use replicate: realistic merchant set with Zipf frequencies and real-style truncations; 12-way imbalanced prototype eval with amount and weekday features. GRAPH-2: label propagation over a kNN graph of all transaction strings vs prototypes by frequency bucket; GRAPH-6: category vector from its name vs centroid. | REAL-1, REAL-4, GRAPH-2, GRAPH-6 | 13, 21 | todo | |
| 35 | REAL-6 evaluation set: synthetic users with their own category schemes (standard, renamed and new names), imbalanced labelled histories with amount and weekday, test cells seen/unseen merchant x seen/new name; frozen and versioned; LLM option scoring and encoder prototype scoring per cell. | REAL-6 | 14 | todo | |
| 33 | The production categoriser with and without an injected fact DB: categoriser trained by label SFT, by contrastive prototypes, and by teacher-trace distillation with the records as a tool (optional GRPO); crossed with no DB, parametric injection of the DB, and record retrieval; the gain on merchants in the DB but absent from the user's history, per REAL-6 cell. | REAL-5 | 35 | todo | |
| 34 | Cloud offload: Modal function for `exp_curriculum.py` / `exp_encoder.py` (image from `uv.lock`, results and adapters synced to DVC); run arm C at 5,000 species x 20,000 steps and Flan-T5 at 1,000 / 5,000 species at 3e-4 there; then every multi-hour row (an H100 is 4 to 6x the 3090 on our training, 80 GB; the remaining queue is about 35 3090-hours or 8 H100-hours, inside the Modal credit). | INFRA-1, REAL-3, MODEL-2 | 18 | todo | |
| 23 | Held-out partition without the weakness-equals-type confound: make weakness independent of type and re-run arm C. | DATA-1 | 2 | todo | |
| 24 | Perplexity on a held-out corpus slice (a few thousand tokens of WikiText-2 test) in place of the single general paragraph; re-score the saved adapters so every arm has the number. | TRAIN-3 | 11 | done | REPORT.md 16; reports/corpus_ppl_Qwen2.5-3B.md |
| 25 | General-text replay: add a few percent of pretraining-style text to arm C's mix and re-run with `PERIODIC=200`; does `K_arc_easy` hold at 73.5 the way ICL replay holds the ICL suite? | TRAIN-7 | 11 | done | REPORT.md 17; results/curriculum_Qwen2.5-3B_Cg_p200.json |
| 26 | Sequence packing for the knowledge stream via unsloth's `SFTTrainer` (`packing=True`, padding-free): verify block-diagonal attention and per-sequence positions on a packed batch, then arm A packed vs unpacked at the same token budget and seed, within the section 9.2 floor on the frozen ladder; report tokens/s, MFU and loss-bearing-token fractions. Belongs right after step 9 in priority (same mixture-by-tokens change); use it for steps 10, 15 and 17 afterwards. | TRAIN-6 | 9 | done | REPORT.md 19. Packed 2,048-token rows (block-diagonal, verified), one 16-sequence micro-batch, batched scorer: eval 26 to 3 min, arm C 30 to 11 min; numbers within the same-seed run-to-run floor. Taken out of order at the owner's request for iteration time |
| 27 | Prompt distillation v2 (after REPORT.md 14): teacher distribution renormalised over the option set, or a teacher with the options listed and scored by letter, at temperature 1; arm P's mixture and budget. Success: bare recall above arm C's 20.6 with the ICL suite held. | BASE-3 | 7 | done | REPORT.md 18; results/curriculum_Qwen2.5-3B_P2.json. Ranking distils (91.9 trained-format recall at +0.03 nats) but stays in its five question forms (yes/no 58.8, pair 50, induction at chance) |
| 28 | Relation-linearity probe: fit a linear relational embedding from the species-name hidden state to the type token on arm C's merged adapter (layer sweep); faithfulness on trained species, transfer to held-out species with and without context; the same for weakness and whether it factors through type. Eval only. | GRAPH-1, DATA-1 | 1 | todo | |
| 29 | Two-hop walk augmentation (EntiGraph-lite): part of the knowledge stream replaced by texts generated from entity-attribute-entity walks at a fixed token budget; pair, yes/no and Timmy from the weights against arm C. | GRAPH-3, DATA-5 | 2 | todo | |
| 30 | Induced attribute-attribute edge: bare and two-hop items for "type T is weak to ?" (never stated in training), scored per adapter; item-set version bump. | GRAPH-4, DATA-1 | 1 | todo | |

## Log

- 2026-09-14: queue created from `references/SURVEY.md` section 2 (which now just points here) and
  the reviewer register in `reports/QUESTIONS.md`. Nothing started yet.
- 2026-09-14: step 1 done (REPORT.md section 9). Item sets frozen as v1 with hashes in the tracker config; every eval writes per-item, per-option log-probs under four premises (prompt, cue only, newline, listed choices) so steps 2 and 4 run without a model. All 11 adapters re-scored on WSL: 111 cells move against the Windows numbers, at most 2.6 points on 160-item levels (the same-weights noise floor). Merging the LoRA flips 1.8% of predictions and halves eval time; adopted for new evaluations, not for EVAL_ONLY re-scores of section 8 arms. Eval per arm is 24 min unmerged, 15 merged.
- 2026-09-14: step 6 done out of order, while step 1's re-score occupied the GPU (it needs no run and
  nothing depends on it). Executive summary and 8.3 carry the weakness-equals-type caveat; "RAG
  ceiling" / "RAG upper bound" are "oracle context" in the report and the code docstrings; 8.1 has a
  sequence / token / loss-bearing-token table (arm C: 84% knowledge text by loss-bearing tokens,
  episodes 12%) and the arm D schedule confound, pointing at steps 9 and 12.
- 2026-09-14: step 2 done (REPORT.md section 10). Elicitation matters more than normalisation: hybrid scoring lifts every induction level of arm C by 5 to 11 points and turns L5 from below chance into 33.8; letter scoring makes knowledge-only arm A the best inducer (62.5) while collapsing the trained arms' ICL suite; PMI is only right for bare-format recall. Several "chance" rows are constant predictors.
- 2026-09-14: step 4 done (REPORT.md section 11). Bootstrap CIs, permutation null bands and paired McNemar tests from the per-item files. Most of section 8 holds; arm D's pairwise loss, C's weakness edge over A and the natural-label half of the replay claim do not. 115 of 336 cells, including the base model's 42.7 on held-out induction (EVAL-6), are inside their gold-blind null band.
- 2026-09-15: step 3 done (REPORT.md section 12). Bare-format recall is about 85% by generation for every knowledge arm (cloze said 20 to 25: the first token after `Answer:` is the species name, not the type); induction by generation matches the cloze within 2 points for the episode arms and agrees with the mean rule on 87 to 95% of items; base and A cannot produce a label (91 to 93% unanswered), so their induction scores, including base's 48.8 with context, are cloze-only. Two section 6 adapters re-decode after the running jobs.
- 2026-09-14: step 5 done (REPORT.md section 13). The casing hypothesis is wrong for the LLM: restoring the merchant name's trained tokens inside the bank string moves 0.5B with-context bank_category from 14.2 to 15.0 (title-casing the whole line: 15.8, chance 8.3); the transaction format, not the tokenizer, is what the small model cannot read. Qwen keeps 10.6% of name tokens under uppercasing, MiniLM (uncased) 96.2% and is indifferent to the rendering (67.7 / 67.7 / 68.8). Step 21's normaliser should strip the format, not just the case.
- 2026-09-15: step 7 done (REPORT.md section 14). Negative: soft-label prompt distillation at T=2 from the base model reading the field guide injected nothing (trained-format recall 13.8 vs base 11.9, induction inside the null bands, ppl 8.64 vs 8.54); the ICL suite's +16 came from the 15% hard-label replay. Measured cause: with the entry in context the teacher puts 6% of its next-token mass on the type token in the training sentence (0.3% at T=2) and the student matched that distribution (KL 2.17 to 0.28). The with-context ceiling is a ranking among options, not a distribution. Row 27 (option-renormalised targets) added.
- 2026-09-15: step 11 done (REPORT.md section 15). Facts are in the adapter by step 200 at a 100% knowledge mix and by step 400 at 45%; arm A's ICL loss (63.5 to 49) is complete at step 200 and 15% replay from the first step prevents it (D 70.8 at step 200 with no episodes yet); ARC-Easy, the new forgetting proxy, drops 11 to 16 points in the first 200 steps in every arm, replay or not, and never recovers; induction from the weights shows up in the last 200 steps and the 40-item subsample cannot watch it arrive; the one-paragraph perplexity swings 2x within a run. Rows 24 (corpus perplexity) and 25 (general-text replay, TRAIN-7) added.
- 2026-09-15: row 26 added (TRAIN-6, new item in QUESTIONS.md). The knowledge stream trains overhead-bound (280 tokens/s, 35 to 60% GPU utilisation for arm A against 850 tokens/s for arm C); packing through unsloth's SFTTrainer would fix that but changes masking and the effective mixture weights, so it is queued as a verified recipe change right after step 9 rather than applied to the running arms. Owner's request.
- 2026-09-15: step 24 done (REPORT.md section 16), taken before rows 8 to 10 because it is a measurement fix every later run should carry: `L7_ppl_wikitext`, perplexity on a frozen 29-paragraph WikiText-2 test slice (4,972 tokens), replaces the one-paragraph number in every evaluation and periodic point. Two runs of arm C agree to 0.03 nats where the paragraph read 14.9 and 30.3. Costs in nats per token over the base (10.6 ppl): A 1.40, Cn 1.08, C/D/E 0.81 to 0.90 (indistinguishable), B 0.37, P 0.086 (5 se from zero); the ICL replay stream's own share is 0.22 (Cn to C). `scripts/corpus_ppl.py` scores every adapter of one base in about a minute.
- 2026-09-15: step 25 done (REPORT.md section 17). Arm Cg = arm C with 5% of sequences (27% of the loss-bearing tokens) as WikiText-2 train paragraphs, PERIODIC=200. WikiText perplexity stays at the base model's (+0.09 nats vs C's +0.84) from the first checkpoint; ARC-Easy 71.0 vs C's 59.0 (+12 paired, p 4e-5; base 73.5); recall 100, ICL suite 81.2 (best arm). Induction from the weights dropped 10 to 21 points on three levels (Timmy 47.5 vs 61.2), confounded with the episode share the general text displaced (12% to 7.5% of the loss). Follow-up folded into step 9 (token-weighted mixture; run the 5% out of the knowledge stream, K .40 E .40 R .15 G .05). Per-stream token and loss-bearing-token counts are now in every run's stats.
- 2026-09-15: rows 28 to 30 added and rows 13, 14, 22 extended from `references/graph_methods.md` (GRAPH-1 to GRAPH-7 in QUESTIONS.md), a literature pass on the owner's observation that the universe is a bipartite entity-label graph. Message passing has nothing to propagate here (functional relations, no entity-entity edges, name strings as the only node feature); what survives is probes of how the graph sits in the weights (28, 30), walk-based data augmentation (29), and label propagation and label-text co-embedding on the merchant side (22). Rows 28 and 30 are eval-only and can run between training jobs.
- 2026-09-15: step 27 done (REPORT.md section 18). Option-renormalised prompt distillation (arm P2, `exp_distill_v2.py`) writes the with-context ranking into the weights: trained-format recall 91.9 (base 11.9, arm P 13.8), bare 28.1 (best arm), general-text cost 0.03 nats (C 0.84), ARC-Easy 77.0, ICL 78.6; the teacher's diet errors (58%) come along. The knowledge stays in the five distilled question forms: yes/no 58.8, pair 50.0, induction inside the null band. BASE-3 closed as answered. A first run was discarded: unsloth's training-mode forward ignores a left-padding attention mask (probe in section 18); the shared option scorer (`scoring.option_logprobs_batched`) right-pads.
- 2026-09-15: step 26 done (REPORT.md section 19), pulled forward at the owner's request ("we are going way too slow"). The scorer batches every option row across items (LM head at option positions only): evaluation 26.5 to 9.1 min with the section 10 extras, 2.7 without (extras now opt-in, EXTRAS=1). Training: one 16-sequence micro-batch packed into 2,048-token block-diagonal rows through unsloth's packed_seq_lengths path (verified: neighbours change log-probs by exactly 0), checkpointing kept: arm A 496 to 1,017 tok/s, arm C 858 to 2,133 (1,863 in the full run). No checkpointing is 30% faster in a bench and died of memory in a full run; not used. An arm C run with periodic points: 63 to 15 minutes. Same-weights re-score flips 1.9% of predictions; the fast-path retrain of arm C sits inside the 17.9% same-seed run-to-run flip rate of the two old-path runs. The remaining queue (rows 8, 9, 10) runs on the new defaults.
- 2026-09-15: step 10 done (REPORT.md section 20). Seeds 1 and 2 of arms A, C, D (PERIODIC=200, fast path; D under plain torch checkpointing after two deaths in unsloth's offloaded checkpointing, now the default). Seed sd: arm A 2 to 4 points on every ladder level; arm C 12.7 on yes/no and 12.6 on pair, 9 to 12 on the induction levels; ICL suite, ARC-Easy and WikiText 1 to 4.5 for all arms. Section 8 gaps that clear 2 sd of the difference: C - A on Timmy k=3 (+27.5, 2 sd 20.2), on the ICL suite (+28.3, 2 sd 5.7) and on WikiText (-19 ppl), D - C on trained-format recall (-8.7, 2 sd 6.7), every arm's -9 ARC-Easy against the base. Not clearing: A over C on manipulation (-18.3 +- 13, -12.5 +- 12.7), "sequential loses" on yes/no (-9.2 +- 15.2), pair and induction, k=4 and weakness for C - A (+23, +18 against 25-point bands). Rule from here: ladder-level gaps between single runs of mixture arms need three seeds or a paired per-item test; general-ability measures read to +-4 from one run.
- 2026-09-15: step 9 done (REPORT.md section 21). Arms M0/M20/M40/M60 weight each stream's mean token loss by its mixture fraction (`BY_LOSS`), with a self-teaching stream S (`universe.self_teaching`) and loss on every inferable demo label (`ALL_ANSWER`). M0 takes arm C's exact batches (same seed; `tok_*`/`lb_*` counters identical) and only changes the weights from the pooled mean's implicit 84 / 12 / 4 to 45 / 40 / 15: trained-format recall 33.8 against 100 in three C runs, manipulation at chance, the curve flat through step 600. The E sweep at the loss level (M20/M40/M60, knowledge weight 65 / 45 / 25%) gives recall 100 / 36 / 13 and manipulation at chance throughout; the only gains are with the field guide in context (M0: 86.2 induction on nonsense names, 82.3 held-out species, 90.6 reverse; arm C 71 to 79 / 71 to 74 / 80 to 83). Self-teaching buys recall without manipulation (M20: 100 / 58.8 / 50.0). TRAIN-1 closed: the implicit 84% is load-bearing; future mixture changes stated in loss weight with knowledge at or above 0.8. The section 17 follow-up (Cg with the general text out of the knowledge stream) is a sampler change and stays queued.
- 2026-09-15: step 8 done (REPORT.md section 22). Qwen2.5-3B-Instruct, LoRA r=64, 800 x 16, one 47-token text per species (136 texts, 94 passes): plain (A1) 65.6 forward recall and backward at chance (21.2 / 20.0); masked (A1m, `encode_masked`, 5-95% of tokens replaced by `<|fim_pad|>`, loss on the reconstruction) 87.5 forward and 60.6 / 48.1 backward on the new frozen set `reverse_v1.json` (320 items; easy = distractors of another type, hard = same type, different diet or region). Costs: symbol-label ICL 51.0 (base 64.6), and with the field guide in context the hard backward level falls from the base's 92.5 to 51.9, the only arm that loses it. Controls: arm A (20 paraphrases) on the same model gives 100 recall, 100 / 92.5 manipulation, 87.5 easy backward (the paraphrase set has type-before-name renderings) and 28.8 hard; inside the section 8 mixture at 43 passes the single text reaches 54.4 plain (C1) and 36.2 masked (Cm), both at chance backward. TRAIN-5 partly: masking removes the need for paraphrases for backward recall, not for manipulation; try masked paraphrases next. EVAL-7: the merchant `reverse` construction is the easy (type-level) level. Instruct C1 died once in unsloth's offloaded checkpointing (as D seeds 1 and 2); reran under plain torch checkpointing, now the default.
- 2026-09-16: step 12 done (REPORT.md section 23). Arms Dr (schedule restarted per phase), Dc (constant rate after warmup), Dk (phase 2 = episodes .75, knowledge .10, replay .15), three seeds each on the fast path. Every variant is at 100 formatted recall at step 400; Dr and Dc then fall to 28.1 +- 16.4 and 41.0 +- 18.8 by step 800 (Dr - D -63, 2 sd 34) and their induction from the weights returns to arm A's level (Timmy 37.7 / 39.4 against D's 59.2): 400 steps of episodes at the full rate with no knowledge text erase phase 1, and arm D's decaying schedule was what held that to the 8.5-point recall gap of section 20. Dk: recall 99.8 +- 0.3 (D 91.5, +8.3 clear), every other level inside arm C's seed band (yes/no 78.3 vs 80.0, pair 75.0 vs 78.7, Timmy 58.7 vs 63.8, ICL 80.2 vs 79.0), WikiText ppl 19.6 vs C's 22.6 (clear). TRAIN-2 closed: interleave, or replay 10% of the earlier phase if staging; never restart the schedule on a phase without the earlier data.
- 2026-09-16: step 13 done (REPORT.md section 24). `exp_embed_block.py` reruns the section 4 and 6 embedding experiments on all-MiniLM-L6-v2, bge-base-en-v1.5 and Qwen3-Embedding-0.6B through sentence-transformers and scores the prototype on the frozen induction items the LLM arms answer (query's nearest demo card), with per-item records paired by id. Trained encoders on the Timmy items: 79.4 / 94.4 / 92.5 against arm C's 61.9 from the weights and 70.6 with the field guide (bge: 54 items right where C is wrong, 2 the reverse); k=4 62.5 / 86.2 / 91.2 vs 51.9; habitat 53.1 / 72.5 / 80.0 vs 29.4 (65.6 with the guide); held-out species at chance for every from-the-weights scorer. BASE-4 baselines (10 splits, 8-way type): frozen encoders at chance, trained centroid 83.7 / 99.4 / 99.9, logistic regression ties it, SetFit 76 / 99 / 92.6. Merchant: ft_subword bank-string transfer 69.8 / 84.4 on the uncased tokenizers and 26.0 on cased Qwen3 (the survey's 'bge is cased' was wrong; it lower-cases), so section 4's subword claim is a property of uncased tokenizers. New tokens (BASE-5: mean, N(mu,Sigma), row warm-up, MOSAIC joint MLM, tied case forms): warm-up best at 55.2 / 76.0, all below the original tokenizer, on Qwen3 the added token never fires on upper-case strings (tied: fires, 13.5). MODEL-5: CLS and last-token pooling fail like mean pooling. GRAPH-5: DistMult scorer over the jointly trained encoder reads held-out category from name at 33.3 / 91.7 / 100 where the encoder's own nearest category text reads 37.5 / 95.8 / 100 and a relation-free pairs control 41.7 / 87.5 / 95.8; the scorer adds nothing. Encoders not saved (each run is under 3 minutes of training); results in results/embed_block_<enc>.json and per_item/.
- 2026-09-16: step 14 done (REPORT.md section 25). `ai_experiments.retrieval`: a fine-tuned MiniLM index over every field-guide entry (held-out species included) and the training texts; contexts `retrieved` (top-3 per name, right or wrong), RAFT (gold with p 0.8 + 2 nearest non-gold, shuffled; training stream Er of arm Cr) and `neighbours` (GRAPH-7: co-typed species per name, no attribute words); RET=1 scores the two new conditions on any arm, ADAPTER_NAME re-scores any adapter. Base, C, P2 and Cr under none / oracle / retrieved / neighbours: arm C is above its no-context number on every level with retrieved context and above its oracle on induction (Timmy 78.1 vs 70.6, k=4 74.4 vs 60.6) because the top-3 are the species' paraphrased texts (retriever: every top-3 document of a seen species is about it; held-out entries top-1 for 87.5%); Cr (10 min, same from-the-weights numbers as C) reads retrieved context best (85.6 / 77.5 / 81.2); the neighbour list is the best context for the trained arms (C 87.5, Cr 93.1 on Timmy; base 36.9) and useless for habitat; P2 with context is below the base on induction and pair (46.9 / 30.6 / 56.2 vs 49.4 / 40.6 / 82.5). Merchants (`exp_retrieval_merchants.py`): record-tuned MiniLM recall@1 100 on every split from bank strings (zero-shot 78 / 88, category-tuned 55 / 29); end to end on untrained Qwen2.5-3B equals the oracle (bank 81.2 / 75.0, name 89.6 / 91.7), top-3 records halve it (46.9 / 41.7), Qwen2.5-0.5B cannot read the record from a bank string (8.3). BASE-1 run, REAL-2 measured (10k records = row 22), GRAPH-7 answered yes. Cr adapter in DVC.
- 2026-09-16: step 15 done (REPORT.md section 26). Arms base, A, C, D on Qwen2.5-1.5B (LoRA) and Qwen2.5-7B (QLoRA, `LOAD_4BIT=1`), same data, steps and hyperparameters, PERIODIC=200, seed 0. Holds at every scale: the mixture's induction gain over knowledge-only (Timmy C - A +26.2 / +21.9 / +18.8 at 1.5B / 3B / 7B), its ICL preservation (+25 / +31 / +31 on symbol labels; A at 47 to 53 everywhere), the forgetting cost (ARC-Easy -11 to -20 for A and C at every scale; WikiText +0.5 to +1.5 nats). Changes with scale: knowledge-only's manipulation advantage is 30 / 18 / 4 points on yes/no (at 7B the mixture is 93.8 / 91.2 against 97.5 / 95.0); knowledge-only at 7B induces from the facts alone (Timmy 55 vs base 33); the sequential arm at 7B loses a third of its facts in the episode phase (recall 100 at step 400, 65.6 at 800; 1.5B and 3B hold 92 to 93), so the section 23 replay is required at 7B. Natural-label ICL flat at every scale. Costs: 1.5B arm 5 to 8 min; 7B QLoRA 10 to 22 min training at 11.5 to 13.9 GiB, evaluation 51 min for the first 7B mixture run (driver spilling VRAM at the 24 GB limit), 6.4 min after the 4-bit scorer budget was cut to a quarter. Single seeds off 3B. Adapters in DVC.
- 2026-09-16: step 16 done (REPORT.md section 27). `universe.knowledge_texts` + `KTEXTS` in exp_curriculum; `gen_llm_texts.py` froze 3,275 Qwen2.5-3B-Instruct sentences (`data/processed/llm_texts_v1.json`). Arm A, 3B, 800 x 16, seed 0: trained-format recall 73.8 / 100 / 97.5 / 100 at 1 / 3 / 7 / 14 templates (the literature's three-per-fact plateau); yes/no and pair at chance for 1 to 14 templates and for the permutation, reverse and LLM variants, 98.8 / 93.8 with the section 8 negatives and comparatives added (`desc14cmp`), so manipulation comes from comparing texts, not paraphrase count; hard backward recall 69.4 only with the four reverse-direction statements (everything else 19 to 32); bare recall peaks at 3 to 7 templates (53 / 64) and falls to 22 once the question-form templates enter; WikiText 20.5 / 23.1 / 28.1 / 51.3 at 1 / 3 / 7 / 14, 83.1 with permutations, 26.7 with LLM prose; ARC-Easy at the base for 3 to 7 templates and 51 to 62 for the 14-template variants. DATA-5 answered: few templates in prose plus the skill-specific texts; sentence-order permutation buys nothing here. Adapters in DVC.
- 2026-09-16: step 17 done (REPORT.md section 28). `LORA_R`, `LORA_TARGETS`, `FULL_FT` knobs; arm C on 3B, seed 0, one change at a time: rank 16 and lr 5e-5 keep recall 100 and drop manipulation to 55 / 50 and 74 / 56 and induction to 43 / 44 (cheaper on WikiText and ARC); lr 2e-4 gives 96 / 85 and 64 / 63 / 65 at the largest cost (WikiText 28.6, ARC 54.5); 400 steps = facts only; 1,600 steps = 97.5 / 91.2 manipulation, Timmy 81.2, k=4 71.9, weakness 76.9, ICL 78.1, ARC 65.5, WikiText 20.9 (all better than the 800-step baseline: the recipe was under-trained); rank 256 = baseline at ppl 31 and 15 GiB; MLP-only r64 = baseline at lower cost; full FT (3.09B bf16, 8-bit AdamW, 19.7 GiB) at 1e-4 erases the model (ICL 60 / 70, ARC 45, ppl 94) and at 1e-5 learns nothing (recall 19.4). Four OOMs in the first backward after a periodic evaluation (r256, s1600, mlp, fullft), rerun with PACK=1024; allocator gc threshold and gc.collect around periodic_eval added. TRAIN-4 answered: LoRA r64 on the MLP projections for 1,600 steps is the recipe to carry forward. Adapters (incl. two 5.8 GB full-FT checkpoints) in DVC.
- 2026-09-16: step 18 done (REPORT.md section 29; section 24.7 addendum). Universes of 1,000 and 5,000 species (three-part names; frozen sets *_n1000, *_n5000; `UNIVERSE_N`): arm C at 1,000 species recalls 51.2 at 800 steps (0.3 passes) and 100 at 4,000 (manipulation 97.5 / 86.2, WikiText 41), at 5,000 species 14.4 / 21.9 at 800 / 4,000 steps; same-exposure comparison 51 vs 22 is the first interference sign; 5k at 1.5 passes (20,000 steps) unrun. `exp_mlm.py` (ModernBERT-large, MLM injection + letter-at-mask protocol): chance everywhere, natural ICL 55.7 -> 39.1. `exp_encoder.py` (Flan-T5-large span prediction, option log-prob + generation, plus the trained span format): ICL suite recovers (72.9) but recall 16.9 / 20 (gen) at 160 species, 13 / 24 at 1k with 1.5 passes, 11 / 16 at 5k; generations show the template with the wrong type. T5Gemma-l-l-ul2 (fetched after the owner's Hub login; no sentinels -> placeholder spans) under full FT at 1e-4 damaged (ICL natural 47, ARC 20). Section 24.7: EmbeddingGemma-300m and gte-modernbert-base at 92 / 93 Timmy, 82 / 78 habitat, 99.9 type; bank-string transfer 77.1 (EmbeddingGemma, cased) vs 21.9 (gte-modernbert, cased): the 24.4 mechanism is the encoder's case robustness, not the tokenizer's casing. Owner's rule: every trained model saved (embed_*, retriever_*, encoder_*, mlm_*). Follow-ups run after the owner freed the GPU (2026-09-17): the learning rate was the whole difference. Flan-T5 knowledge-only full FT at 3e-4 recalls 98.1 (option) / 100 (generation) in the trained format, bare recall 40.0 (decoder C 19.4), Timmy 30.6 (base 35.6, C 61.9: no induction), natural ICL 75.0, ARC 30.5; 1e-3 recalls 100 with natural ICL 52.6, 4,000 steps at 1e-3 100 with ICL 45.8 / ARC 22; every 1e-4 Flan-T5 run (160 / 1k / 5k) was under-trained, and the 1k / 5k runs at 3e-4 are unrun. T5Gemma with LORA=64 at 1e-4 (mixture) recalls 97.5 / 93.1 (gen), bare 42.5, natural ICL 43.8, ARC 39.5, 4.8 GiB; at 3e-4 48.8 with more loss. Flan-T5 LORA=64 at 3e-4 learns nothing (11.9); the adapter at 1e-3+ is unrun. Span-format re-scores of the 160-species Flan-T5 runs: base 10.0, mixture 13.8, so the format was not the obstacle. Qwen3-Embedding and merchant-retriever saving re-runs done (RUN_TAG=saved). MEMIT smoke for row 19: EasyEdit needs `lm_head_module: model.embed_tokens` on Qwen2.5-3B (tied embeddings; `LookupError: lm_head.weight` otherwise), fixed in both hparam files; the smoke with the fix edits 4 species' types with EasyEdit rewrite accuracy 100 in 20 minutes (the WikiText covariance statistics for layers 4-8 dominate; they are cached for the full run). Five runs OOMed after periodic points at 2,048-token rows earlier in the day; PACK default is 1,024.
- 2026-09-17: rows 31 to 34 added from the owner's reading of the qorl post and the training services (`references/task_training_and_services.md`; QUESTIONS.md TRAIN-8, TRAIN-9, REAL-5, INFRA-1). Placed by expected information per GPU-hour: 31 (on-policy distillation repair, the Thinking Machines fix for exactly our forgetting numbers) and 32 (LoRA at 10x the rate, two to four cheap runs that may overturn the adapter verdicts of sections 28 and 29) go before row 20; 33 (task-direct merchant training) after 22, since it is the owner's actual use and needs the merchant machinery of rows 14 and 22; 34 (Modal offload) after 33 in the table but can run as soon as someone sets it up, and it unblocks the long REAL-3 / MODEL-2 runs.
- 2026-09-17: owner's final goals recorded (a fine-tuned transaction categoriser that handles unseen category names and unseen related merchants; external merchant / POI fact DBs injected to improve it on merchants the user never labelled). REAL-5 rewritten around that and REAL-6 added as its evaluation set; row 35 (the eval set) precedes row 33 (the categoriser x fact-DB experiment), which now needs it.
- 2026-09-17: step 19 done (REPORT.md section 30). EasyEdit on Qwen2.5-3B (checkout beside the repo; driver, hparams and library patch under `scripts/easyedit/`): `lm_head_module` must be the embeddings (tied), `batch_edit` in one chunk with `sequential_edit=True` is the batch protocol (`edit` applies and undoes edits one at a time, 22 h for 136), MEMIT's closed-form solve explodes on Qwen's near-singular WikiText covariance (half the eigenvalues < 1e-6; ppl 5.7e7) and a ridge on the solve (`cov_ridge` 15 or 150) fixes it. Results: one type edit per species (136 edits, 26 to 34 min): AlphaEdit and MEMIT+ridge recall 100 trained-format and 100 on the plain type question (base 18, A 25, C 20), seen-recall control 92 to 96, WikiText 10.7 to 10.9 (base 10.6), ARC 72, natural ICL 86 to 87; yes/no 55 to 61, pair at chance, Timmy at the base, reverse at chance; the type edit answers the weakness question at 0.0. Five facts per species: subject-first AlphaEdit rewrite 20.8, per-fact plain recall 16 / 21 / 25 (type / weakness / habitat); relation-first clauses (MEMIT+ridge) 46 / 36 / 69; relation-first records 25 / 14 / 81; subject-first records keyed at the field token (the `last` key path implemented) rewrite 6.7 in a two-species smoke, full run not made. Untried: one target vector per species jointly over its prompts; AlphaEdit with relation-first prompts. Bare recall is now split by attribute in `scripts/edit_tables.py` (the pooled number hid the 100 / 0 pattern). Owner's reading of the qorl post and the training services folded in the same day (rows 31 to 35).
- 2026-09-18: step 31 done (REPORT.md section 31, TRAIN-8). On-policy distillation from the adapter-off base repairs arm C's general measures to the base exactly (ppl 10.58, ARC 73-75.5) and erases the facts with them (recall 100 to 35 by step 30; the adapter keeps its norm and rotates, cosine 0.82); with one micro-batch of arm C's mixture per step it keeps the facts (recall 96.9, yes/no 77.5, Timmy 61.9) at ppl +0.04 nats, ARC 73.5, ICL 82.3 / 90.1, the best injected adapter in the report and better than Cg on every measure, at 318 GPU minutes (218 sampling with Hugging Face generate) against Cg's 29. Three attempts of the replay run died before one finished (unsloth's fused loss sizing itself from free memory; the allocator reserve at the 24 GB ceiling between the generate and train phases); both fixed in the script. Not run: WikiText-train prompts (stopped at step 15), the sampled-token estimator, a replay-weight sweep. Follow-ups worth a row later: the repair on the 1,000 / 5,000-species adapters (section 29's largest losses) once a faster sampler exists (row 34, Modal, or vLLM locally).
