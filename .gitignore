.venv/
__pycache__/
*.pyc
hf_cache/
*.log
unsloth_compiled_cache/
*.out.txt
evals/runs.db
evals/runs.db-wal
evals/runs.db-shm

# paper archives (keep paper.txt, summary.md, meta.json)
references/papers/*/*.pdf
references/papers/*/*.tar.gz
references/papers/*/src/
references/papers/*/paper_flat.tex

# per-user Claude Code overrides (the shared settings.json is tracked)
.claude/settings.local.json

# smoke-run outputs (`just smoke`): plumbing checks, not results
results/*_smoke.json
